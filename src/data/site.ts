/** Site-wide brand constants. Single source of truth for the company name. */
export const BRAND = 'Art Color';

/** Default fallback name when product data is unavailable. */
export const BRAND_FALLBACK = 'Art Color';

/**
 * Production canonical URL.
 * TODO: replace with the real domain (e.g. 'https://artcolor.vn') before deploy.
 * Keep in sync with `site` in astro.config.mjs and the Sitemap line in public/robots.txt.
 */
export const SITE_URL = 'https://example.com';
